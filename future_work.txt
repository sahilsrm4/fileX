we have to build a data structure which can store response of every tool like directory summary , list files, file information 
so that we can print it directly from main file and also pass it to ai_application
we have to handle the custom output object in directory summary recrusive calls
